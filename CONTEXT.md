# Live2DAction Flight System

Movement and combat systems for a third-person open-world action game (Unity), with an open-world traversal layer inspired by Genshin Impact / Wuthering Waves.

## Language

**Flight**:
The player's freely-controlled airborne movement mode — hold the flight key to ascend, hold the descend key to sink, release both to hover in place. Fully overrides gravity while active; ends on landing or on Flight Energy depletion (see Glide).
_Avoid_: Flying, gliding (Flight is the *active, energy-costing* mode; Glide is its fallback, a distinct state)

**Glide**:
The fallback airborne state entered when Flight Energy depletes mid-flight — a soft, reduced-gravity descent (not a hard fall), with horizontal movement still fully controllable. Costs no Flight Energy — Flight Energy regenerates normally while gliding, so Flight can be re-entered once it refills. Ends on landing.
_Avoid_: Falling (Falling is the normal, full-gravity airborne state outside of Flight/Glide entirely — e.g. walking off a ledge without ever having flown)

**Flight Energy**:
A dedicated regen-over-time resource, separate from the Ultimate skill's own energy, that Flight drains while active. Reaching zero ends Flight and starts Glide.

**Decorative Wings**:
A rigidly-attached, non-gameplay wing mesh with a procedural flapping animation, worn by a character for visual characterization only — grants that character no Flight/Glide ability and requires no AI changes. Used by the player (functional counterpart, worn alongside real Flight). Player4's mechanical wings were Decorative Wings only at first, but see Aerial Combat below - that scope decision has since been superseded.
_Avoid_: Flight wings (misleadingly implies functional flight, which Decorative Wings explicitly do not grant)

**Aerial Combat**:
Player4's AI-driven airborne engagement mode, entered reactively when the player is significantly higher than Player4. Distinct from the player's own Flight/Glide (no Flight Energy, no player-key-driven ascend/descend) - Player4 hovers/chases using its own simplified vertical-velocity control instead. Melee attacks during Aerial Combat reuse the player's existing EnemyAttack1-4 data unchanged, just with a spherical (not capsule) hit-judgment shape for vertical tolerance.
_Avoid_: Flight (Flight specifically means the player's energy-costing, key-driven mode - Player4 never has Flight Energy and isn't Flying in that sense, even while airborne fighting)

## Decisions

- **Takeoff**: No dedicated takeoff/transition animation — Flight begins the instant the flight key is held (matches Wuthering Waves' instant-lift-off model, not Genshin's jump-then-deploy-glider model).
- **Camera**: `ThirdPersonCameraController` pulls its follow distance back (~1.3–1.5×) while `IsFlying`, still subject to the existing obstruction-avoidance clamp. Angle/FOV unchanged for now.
- **Dive-to-build-momentum**: explicitly deferred. Flight stays a flat ascend/descend/hover model (no momentum tracking) until the basic version has actually been played and confirmed to feel right.
- **Player4's Decorative Wings flap rate**: tied to `EnemyAI.CurrentState` (idle rate normally, faster/harder flap while `Attacking`) rather than a constant rate — reuses `WingFlap`'s existing two-tier speed design, just driven by a different source than the player's `IsFlying`.
- **Aerial Combat partner**: Player4 (identified by its own large `detectionRange`, 8, vs 076's 3) — supersedes the earlier "Player4 is Decorative-Wings-only, no aerial AI" scope decision above. 076/077 explicitly excluded (Live2D billboard characters - flying them through 3D space would look wrong).
- **Player4's airborne movement**: an independent, simplified AI hover/vertical-chase (own vertical-velocity control, same shape as the existing `ApplyUpwardLaunch` hook) - does NOT share the player's Flight/Glide/Flight Energy system. No energy limit; AI just needs to reliably reach the target altitude.
- **Aiming at a vertically-offset target**: two changes together, not one alone - (a) characters gain real pitch rotation when facing a locked/aerial target, clamped to ±60°, eased the same way existing yaw turning already is; (b) melee hit-judgment switches from the standard forward capsule to a sphere centered on `attackOrigin` for vertical tolerance, so a slightly-off pitch angle still lands.
- **Spherical hit-judgment scope**: Aerial Combat only. Ground melee (both Player4's and the player's) keeps the existing forward-capsule judgment exactly as tuned - not touched by this feature.
- **Aerial Combat trigger**: player is >3m above Player4 (roughly 1.5-2 character heights) while Player4 can otherwise detect them. Below that, Player4's existing ground chase/attack logic is completely untouched.
- **Aerial Combat chase ceiling**: 15m. Beyond that Player4 abandons the chase, drifts back down to the ground, and waits to re-trigger if the player re-enters range. Purely a safety valve against the AI chasing the player out of the level/into the sky island's special geometry.
- **Aerial Combat attack data**: reuses `EnemyAttack`/`EnemyAttack2/3/4` unchanged (same damage/frame data) - only the hit-judgment shape changes (see above), not the moveset. Matches the explicit request that melee stays the primary combat mechanic in the air.
- **Aerial Combat exit threshold ≠ enter threshold**: real playtested bug - the first implementation exited Aerial Combat at the same 3m band used to enter it, giving zero hysteresis. That produced a rapid enter/exit bounce right at 3m (climb under aerial control → hit 3m → drop to gravity → fall → gap grows past 3m → re-enter → repeat), which read as an erratic flight trajectory and, worse, meant Player4 always disengaged the vertical-tolerant spherical judgment 3m short of actually reaching melee range - it could never get close enough to land a hit. Fixed by exiting only once the height gap has closed to within Player4's own effective attack range (not the 3m entry band) - it now keeps climbing/descending under aerial control all the way down to real melee distance before handing off to ground combat.
- **Pitch never rotates the CharacterController's own transform, for either character**: second real playtested bug, found right after the one above. Both CharacterMovement and EnemyAI originally applied their pitch angle directly to `transform.rotation` - but that's the SAME transform the CharacterController capsule lives on, and the capsule's "up" axis follows the transform's local Y axis. Pitching it doesn't aim the head, it physically tips the whole collision capsule over: visibly flickered between standing and lying flat as pitch changed frame to frame, and the tilted capsule's collision response fought vertical climb enough that Player4 could never gain net altitude on the player. Fixed by keeping the CharacterController's own transform yaw-only always, and instead applying pitch to the "Visual" child transform's local rotation (holds only the Animator, no CharacterController) - same visual look-up cue, capsule never leaves upright. (A related sign bug was caught and fixed in the same pass: `Quaternion.Euler(+X,0,0)` tips forward DOWN in Unity, opposite of AimUtility's "positive pitch = looking up" convention - the original code had never negated for this, so the player's `_visual` now correctly does `Euler(-pitch, 0, 0)`.)
- **Aerial Combat pitch is cosmetic only, not attack-judgment-relevant**: confirmed by the fixes above - `UseSphericalJudgment`'s hit check is a position-only sphere with no facing dependency, and the exit-hysteresis fix already guarantees `_isAerialCombat` is false (so facing has already reverted to horizontal) by the same frame an attack could ever land. Visual pitch exists purely so Player4 visibly looks up/down at the target mid-chase - it was never load-bearing for whether hits actually register.
- **Flight resume from Glide needs its own hysteresis too**: real playtested bug ("飛行有能耗 非一半就停下來導致飛行軌跡奇異"), same family as the Aerial Combat one above but on the player's own Flight/Glide toggle - this project's tuning has Flight draining energy faster (20/sec) than it regenerates (10/sec), so the original "resume Flight the instant CurrentEnergy > 0f" check let a single regen tick (10 energy) immediately re-enter Flight, drain back to 0 in 0.5s, and drop back to Glide - a repeating hop/glide stutter once energy first ran out, not a clean single transition. Fixed with `flightResumeEnergyThreshold` (30, i.e. Glide must bank a real reserve, not just >0) before Flight can resume - mirrors the Aerial Combat exit-threshold fix's own reasoning (asymmetric enter/exit values to kill an oscillation at a shared boundary).
- **Aerial Combat must keep tracking through its own swing, not just up to it**: real playtested bug ("敵人飛行時如果攻擊玩家會停留在原地 導致跟不上玩家 最後一直卡在飛行-攻擊落空-飛行的狀態") - the exit-hysteresis fix above stopped it from bailing out early, but once `CurrentState == Attacking` it still froze in place entirely (ground melee's own "target isn't going anywhere mid-swing" assumption, which doesn't hold against a fully 3D-mobile flying player). Fixed by (a) not exiting Aerial Combat while `combat.CurrentPhase != Idle` (a swing genuinely in progress), and (b) letting horizontal chase continue during Attacking too whenever `_isAerialCombat` is true - so it keeps adjusting position for the swing's ENTIRE duration and only actually lands once both close AND between swings.
- **Execution damage is now a fraction of CurrentHealth, not MaxHealth**: changed from the original "50% of MaxHealth" decision (explicit user request, "斬殺時改為扣除對方當前血量50%") - an execution now always takes a proportional bite out of however much fight is actually left in the target, rather than a fixed MaxHealth-based amount that becomes a shrinking fraction of remaining HP as a fight wears on. Renamed field on both ExecutionAbility/EnemyExecutionAbility to `executionDamagePercentOfCurrentHealth` to match.
- **Execution Ready Indicator**: a world-space red glow ring + blinking core dot, procedurally generated (not hand-authored art) and placed at chest height (offset forward off the root pivot so it renders in front of the torso instead of inside it - a real bug the first screenshot caught), driven by `StancePoise.IsStaggered` directly - visible only during the exact window a deathblow can land, on every character that has StancePoise (derived from the component itself, not a hardcoded name list).
- **Player4's Aerial Combat stats are 1.2x/1.5x the player's own flight stats, computed once, not live-coupled**: explicit user request ("讓敵人能夠飛得比角色高1.5倍 飛行速度比玩家快1.2倍"). `aerialCombatChaseCeiling` (45m) = 1.5x the player's own max CONTINUOUS Flight climb distance in one energy burst (flightAscendSpeed × maxEnergy/drainPerSecond = 30m at this project's tuning); `aerialVerticalSpeed` (7.2) = 1.2x the player's `flightAscendSpeed` (6); new dedicated `aerialHorizontalSpeed` (2.4) = 1.2x the player's `moveSpeed` (2), kept SEPARATE from Player4's own ground `moveSpeed` so the boost is flight-only, not also a ground-combat speed buff. Hardcoded numbers with the ratio reasoning in a comment, not a runtime cross-reference to CharacterMovement's fields - matches this session's existing precedent for every other direct-request stat tune (health/damage percentages etc.), and avoids a live coupling that would silently need to stay in sync if the player's own flight stats are retuned later.
- **Aerial Combat only truly exits once actually grounded, not merely "close enough"**: real playtested bug ("敵人擁有只會飛到玩家下方...請讓他一定要飛到玩家的身邊") - closing the height gap to melee range used to exit Aerial Combat outright (between swings, per the midSwing fix above), handing off to gravity with nothing underneath it in mid-air - so it spent most of an engagement sinking below the player and re-climbing rather than settling in beside them. Exit condition changed to `_controller.isGrounded` (plus the ceiling safety valve) - it now keeps hovering at the player's own altitude for the entire time it's airborne near them, only actually landing (and reverting to gravity) once the player themselves come back down and it genuinely touches ground.
- **Player4's Visual child never pitches, period**: follow-up real playtested bug ("敵人仍然斜斜的 要像玩家一樣站直才行") - a still-earlier fix had moved the aerial "look up/down at the target" pitch off the CharacterController's own transform onto the Animator-only "Visual" child instead, reasoning it was harmless since it never touched the collision capsule. That held while Aerial Combat only stayed active briefly (closing the gap) - but once the hover-stability fix above made it stay active for a WHOLE engagement, the same pitch became a near-constant visible tilt instead of an occasional look-up flourish. Removed entirely - Player4's body (both the root AND the Visual child) now stays fully upright always, matching how the player's own body reads (the player only ever pitches while deliberately locked onto a vertically-offset target, a rare gated action Player4 has no equivalent of). Also had to hard-reset Visual's `localRotation` back to identity once by hand - the many rounds of reflection-based verification this feature went through had left a stale non-zero rotation actually saved in the scene, which nothing would have self-corrected once the pitch-writing code was deleted.
- **Aerial vertical tracking is constant-rate pursuit (MoveTowards), not proportional decay**: follow-up real playtested bug ("敵人還是有機會卡在玩家下方且極限攻擊距離外導致空打") - the original vertical law was `velocity = clamp(heightDiff, -speed, speed)`, i.e. an exponential decay (~1s time constant) that slows down the closer it gets. Fine against a stationary target, but against a target that keeps repositioning (the whole premise of Aerial Combat), the last stretch of distance never actually finished closing - Player4 would sit at a small-but-nonzero residual gap indefinitely, often just outside effectiveAttackRange, causing whiffs. Switched to `Mathf.MoveTowards`-based pursuit: full aerialVerticalSpeed every frame until the remaining gap is smaller than one frame's own step, at which point it closes exactly (no overshoot, by MoveTowards's own guarantee) instead of asymptotically trending toward zero forever.
- **Two more edge cases found by a general review pass (not user-reported, caught proactively)**:
  - Aerial Combat's exit condition only ever checked vertical distance (ceiling) and grounded state, never horizontal distance - a player flying far away horizontally while staying elevated left Player4 with `CurrentState==Idle` (horizontal chase already frozen) but `_isAerialCombat` stuck true forever, hovering motionless in mid-air indefinitely instead of giving up and landing. Added `distance > detectionRange` as a third exit trigger (same `!midSwing` gate as the grounded check, so an already-committed swing still finishes).
  - Getting staggered while genuinely airborne (which Aerial Combat now makes common, given the hover-stability fix above keeps Player4 airborne for most of an engagement) used to still fall under gravity - horizontal velocity already froze while staggered (matches "stand frozen and open" intent) but vertical never did, so a "kneeling, about to be executed" enemy would plummet mid-pose instead of actually holding still. Vertical velocity now hard-freezes to 0 while staggered, airborne or not.
- **Health rebalance + slower stance growth**: explicit user request ("玩家血量改為 500 敵人改為400 同時減緩架勢條成長速度"). Player MaxHealth 300→500, Player4 MaxHealth 100→400. Stance/poise growth is now a separate rate from raw damage dealt - new `stanceGainMultiplier` (0.5, applied to every StancePoise-bearing character: Player/Player2/Player4) scales incoming damage before it's added to the poise bar, so a stagger now takes roughly twice as many hits at this project's common 10-damage attacks (6→12 hits to fill a 60 maxStance bar) - deliberately a separate multiplier from `maxStance` itself, since raising `maxStance` would also change how much accumulated damage a stagger represents, not just how fast it fills. Execution damage (percent of CurrentHealth) needed no corresponding change - it already scales automatically with whatever the target's current health happens to be.
- **Death Animation (Dying.fbx)**: explicit user request ("將這個動作作為所有角色死亡時的共同動作"). Real architectural conflict this surfaced: `Health.ApplyDamage` used to call `gameObject.SetActive(false)` SYNCHRONOUSLY in the same call that fires `Died` - meaning nothing ever got even one frame to render a death animation before the whole GameObject (and its Animator) vanished. Fixed with a new `Health.deferDeactivationToDeathAnimation` bool (default false, preserves the original instant-deactivation behavior for anything that doesn't opt in) - when true, `DeathAnimationLink` (living on the character, wired via a new `DeathAnimationSetup` bootstrap script) triggers the Animator's "Dead" state and deactivates the GameObject itself after the clip's own measured duration (3.5s) instead. Only wired onto Player/Player3/Player4 (the three sharing Maya's/Arisa's Humanoid Animator Controllers, both of which got a new terminal "Dead" state with no return transition to Locomotion) - Player2 (no Animator at all) and 076/077 (Live2D Cubism billboards, a different animation system entirely) keep the original immediate-deactivation path since a Humanoid FBX clip isn't applicable to either rig type. CharacterMovement/EnemyAI/PlayerCombat all needed a `health.IsDead` freeze added alongside their existing `staggered` freeze (a dead-but-not-yet-deactivated character would otherwise keep moving/attacking for those 3.5s) - kept as a SEPARATE `frozen = staggered || isDead` gate in EnemyAI specifically (not merged into `staggered` itself), since `CurrentState == Staggered` is StancePoise-specific AI-FSM bookkeeping that a dead enemy isn't actually in.
- **Uniform basic-attack damage (25) across Player/Player4/076**: explicit user request ("將玩家 敵人 076的普通攻擊傷害改為25 每段都是"). Every combo step's AttackData - LightAttack1-4 (Player, shared with Player3), EnemyAttack/2/3/4 (Player4), GiantAttack1/2/3 (076) - set to Damage=25 uniformly. EnemyUltimateAttack (30) deliberately left untouched - it's the ultimate skill, not a "普通攻擊" (basic attack), so it wasn't in scope.
- **Execution deferred-damage timing reconfirmed, not a new bug**: user asked to ensure execution damage only lands after the animation finishes, "所有人套用" (apply to everyone). Both `ExecutionAbility` (player→target) and `EnemyExecutionAbility` (Player4→player) already implemented this correctly from earlier session work (`_elapsed < executionAnimationSeconds` gates `ResolveExecution`) - reflection-verified both directions rather than assuming: health stays unchanged while pending and elapsed is far below the threshold, and only actually drops once elapsed crosses `executionAnimationSeconds`. No code change was needed here.
- **Execution Ready Indicator size/position, real playtested bug ("這個處決的紅圈提示太大了 而且沒有定位在角色胸口")**: two independent root causes, both fixed together. (1) Ring/dot size (0.42/0.15) was a single fixed absolute value shared by all three characters despite very different builds - it worked reasonably on Player but was ~87% of Player4's actual body width, engulfing shoulder-to-shoulder instead of reading as a chest marker; shrunk to 0.2/0.07. (2) Position was independently RE-MEASURING `HealthBarSetup.MeasureVisualTopLocalY` every setup run, but Player's "Visual" hierarchy now includes a large held sword prop (the "Genshin sword scene decoration") whose renderer bounds extend well past the actual head - a fresh measurement landed the indicator up at the head-level bar stack's own height instead of down at the chest. Fixed by reading the position back out of the EXISTING `WorldSpaceHealthBar` (a known-good, already-correct reference) instead of re-measuring from scratch, same fallback-chain pattern `StanceBarSetup.ResolveStanceBarLocalY` already uses. Re-verified via screenshot on all three characters (Player/Player2/Player4) this time, not just one.
- **Execution Ready Indicator, real playtested regression ("目前看不到紅圈提示")**: the "capture whatever color's currently on the Image as the 100%-visible reference" pattern (already bitten once by a setup-script-zeroed-alpha version of this same bug) broke again a different way - this time the scene got saved mid-session while genuinely not staggered (alpha correctly 0 at save time), and reopening the scene resets the capture flag, so the NEXT Update() recaptured that saved 0 as the new permanent "base", multiplying by 0 forever after. Root design flaw: there was never actually a meaningful "base color" to preserve here - the sprite textures themselves already bake in the red/glow color, so the Image tint is always meant to be identity white. Removed the capture-and-multiply pattern entirely; alpha is now written directly from the pulse/blink functions' own output (already the intended final alpha, not a multiplier) every frame.
- **Post-stagger grace period, real playtested bug ("硬直觸發後架勢條來不及歸0馬上受到攻擊又進入硬直")**: `EndStagger()` already zeroed the bar instantly (confirmed correct earlier this session) - the actual problem is that "zero" isn't "safe" once basic attacks deal 25 damage (this session's own damage rebalance): 25 × the 0.5 stanceGainMultiplier = 12.5 poise/hit against a 60 maxStance bar, so just 5 hits anywhere in an uninterrupted combo instantly refills it again, often before the character has even finished standing up. New `postStaggerGraceSeconds` (1.5s) on `StancePoise` - incoming hits during this window still deal their normal HEALTH damage (nothing about `Health.ApplyDamage` changed), only STANCE accumulation is skipped, giving a guaranteed beat of breathing room after every stagger before poise can start building toward another one.
- **Execution Ready Indicator, chest-bone tracking so it survives kneeling ("蹲下時準確地落在胸口 所有角色都一樣")**: real playtested bug - the indicator's world position was a fixed local offset computed once at setup time (standing pose), so it stayed pinned to that offset regardless of the character's actual current pose; the moment a staggered character plays `KneelingDown.fbx`, the body drops but the fixed offset doesn't, leaving the marker floating up around the shoulders/head instead of the chest. Fixed with a new `positionAnchor` field on `ExecutionReadyIndicator`: for any character with a Humanoid `Animator` (Player/Enemy, resolved via `animator.GetBoneTransform(HumanBodyBones.Chest)`), the indicator now recomputes its world position every `LateUpdate` - after the Animator has finished evaluating the frame's pose - as `chestBone.position + (unit vector toward camera) * chestSurfaceOffset(0.15)`, so it rides the real chest bone through any pose, standing or kneeling. Mecha (no Humanoid Animator, never leaves its standing pose) keeps the old fixed root-relative offset since there's no bone to track.
- **Execution Ready Indicator redesigned around a heartbeat, not independent breathing/blink ("心臟 人的核心部位，你要做出那種要被處決的恐懼感，紅色 鮮豔 脈動")**: explicit user request to replace the original design's separate sine-wave "breathing" pulse on the ring and blink on the dot with something that reads as an actual heartbeat - fear, not calm. New `ExecutionIndicatorPulseUtility` drives Ring and Dot off one shared curve so they beat in sync: 110 BPM (faster than a resting ~70, deliberately tense) with a real double-bump "lub-dub" per cycle - two Gaussian peaks at phase 0.08/0.22 (widths 0.045/0.05, second peak at 0.65x the first's strength) separated by a long quiet gap, not a uniform sine wave. Dot swings wider than Ring (alpha 0.2-1.0, scale 0.8-1.35 vs Ring's 0.55-1.0/0.94-1.08) since the Dot represents the heart itself and the Ring is just the targeting frame around it. Replaces the old independent `ringPulseSpeed`/`dotBlinkSpeed` fields entirely; Image tint stays fixed identity white per the fix two entries up - the heartbeat function's own output is written directly as alpha, never captured/multiplied.
- **Scene data drift caught by audit: the chest-tracking/heartbeat code above existed but had never actually been applied to the scene**: found proactively (not a fresh user bug report) while re-verifying this feature - `ExecutionReadyIndicator`'s C# had already been rewritten with `positionAnchor` and the heartbeat curve, but the three characters' actual serialized components in `GreyboxTest.unity` were still frozen on the OLD version's fields (`ringPulseSpeed`/`dotBlinkSpeed`, no `positionAnchor` at all) - nobody had re-run the setup tool since the code changed, so the kneeling-position bug above was still live in the actual playable scene despite the code fix being correct. `ExecutionIndicatorSetup`'s `Tools/Live2DAction/Add Execution Ready Indicator To All Stance Characters` menu item destroys and rebuilds the indicator fresh on every StancePoise character; re-running it picked up both the bone anchor and the heartbeat parameters correctly - verified via MCP component inspection afterward (`positionAnchor` = `Bip001-Spine1` on Player, `Chest` on Enemy, `null` on Mecha as expected; `heartbeatBPM: 110` and matching alpha/scale ranges on all three). General lesson: a MonoBehaviour code change alone does not reach GameObjects already saved in a scene - their serialized field values only update when something actually re-touches those instances (here, re-running the bootstrap setup tool).
- **Execution Ready Indicator, faint square frame around the ring/dot ("硬直紅圈特效外邊有的透明正方形框 請移除")**: pixel-inspected both PNGs directly (`ExecutionRing.png`/`ExecutionDot.png`) - their alpha channel is genuinely 0 everywhere outside the circle/tick shapes, so the square wasn't baked into the source art. Root cause is a `UnityEngine.UI.Image` default: `useSpriteMesh` is false unless set otherwise, so Image ALWAYS renders a full rectangular quad sized to the RectTransform regardless of the sprite's own alpha shape or its Tight sprite-mesh import setting - normally invisible since alpha=0 there, but GPU texture compression on import (Standalone defaults to a compressed format) can lift those supposedly-zero corners just barely above 0, enough to read as a faint square once anything downstream amplifies it. Fixed at the geometry level, not the compression level: `CreateCenteredImage` now sets `image.useSpriteMesh = true`, so uGUI builds the quad from the sprite's already-Tight import mesh instead of the full rect - the transparent corners have no geometry at all any more, immune to the artifact regardless of exactly how much the corners' alpha drifted. Re-ran the setup tool (same "code fix alone doesn't reach the scene" lesson as the entry above) and verified via MCP component inspection: `useSpriteMesh: true` confirmed on both Ring and Dot Image components post-rebuild.
- **Execution Ready Indicator, remaining "square frame" was never a render bug at all**: after the `useSpriteMesh` fix above, the user still saw a faint square outline around the ring/dot in the Scene View specifically - traced to Unity's own Scene-View gizmo for `RectTransform` (the dashed corner-handle outline every UI RectTransform draws in-editor), not anything in the actual rendered output. Disabled via `UnityEditor.AnnotationUtility` reflection (`SetGizmoEnabled(classID: 224, "", 0, false)` - 224 is RectTransform's built-in class ID) rather than any change to the indicator's own assets or components, since there was nothing left in either to fix.
- **HealingSpring heal rate raised to 100/sec**: explicit user request after asking why the stance bar filled faster than expected during mutual combat (separate, already-understood mechanic - `stanceGainMultiplier` scales per-hit, unrelated to healing) - user picked 100 (up from 40) via a direct choice.
- **Player `StancePoise.maxStance` raised to 100**: explicit user request ("幫我把玩家的架勢條改為100"), Player only - Mecha/Enemy/中立者1-3 all kept their existing values, not a global rebalance.
- **Invulnerability Ripple effect added, then made much more visible**: explicit user request ("我忘記給無敵狀態設計特效了 你發想創意看看") for a new visual on `Health.IsInvulnerable` (covers both the 0.2s dodge burst and the 1.5s post-stagger grace window, same flag either source sets) - user picked "銀白能量漣漪" (silver-white energy ripple) from three presented directions. First pass (single ring, pale silver-blue, chest-badge-sized) read as too subtle/small; user follow-up ("特效不是很明顯 請覆蓋整個身體 像水滴滴到合理波浪展開的感覺") drove a rebuild around 3 simultaneously-running rings at staggered phase offsets (real water-drop-splash reads as several concurrent waves, not one ring blinking), a much larger scale range (0.25-4.5x, envelops the whole silhouette not just the chest), a more saturated cyan (not pale silver), and a slower `Pow(fade, 1.3)` alpha falloff instead of the original quadratic fade. New `InvulnerabilityRippleUtility`/`InvulnerabilityRippleEffect`/`InvulnerabilityRippleSetup`, applied to every `StancePoise`-bearing character (6 total).
- **Checkpoint time-trial course relocated from the sky island itself to the actual ground↔sky-island flight route, plus a new boost-on-touch mechanic**: explicit user request for a Genshin/Wuthering-Waves-style obstacle/climbing time trial ("3D動作遊戲不是常常有那種 障礙物跨越 或是跳躍爬高的比賽嗎 怎麼設計"), refined via two rounds of clarifying questions - "單純計時衝刺" (pure time trial, no resource gating) and an initial "現有空島路線" answer that was misread as "build gates clustered around the sky island itself". The first build (5 gates near `Portal_GroundToSky`'s own arrival point) turned out not to match what the user actually meant; their real intended route, given explicitly once the mismatch surfaced ("你要把光環放在空島與平地之間的路線，我會用上升氣流到空中，從空中到空島的這段路線 每隔一段距離放一個光環，光環碰到時玩家會短暫被加速"), was the OPEN-AIR path between `Updraft_MainArea` (ground level, ~(-2.5,0,-6)) and the sky island (~(-70,22,-25)) - confirmed flyable over `GreyboxSceneBuilder`'s own 6-unit boundary walls first. `SkyIslandTimeTrialSetup`'s gates were relocated to 6 points along that ~70-unit path (every ~10-14 units, easing from y=7 near the updraft up to y=23 at the island), and every gate is now ALSO a speed-boost pad: new `CharacterMovement.ApplyBoost(multiplier, durationSeconds)` (same "external trigger nudges an existing velocity system" shape as `Updraft.ApplyUpwardLaunch`, multiplies `moveSpeed` for a duration rather than adding a flat speed) called from `CheckpointGate.OnTriggerEnter` on every touch (2x speed, 1.5s, both refreshable by flying through another gate before the first expires) independent of whether `TimeTrialController` currently counts that particular touch as run progress.

- **Checkpoint course follow-up, explicit user request ("經過光環時需要有向前短距離衝刺的作用")**: `CheckpointGate.dashSpeed`/`CharacterMovement.ApplyDash` replaced the earlier `ApplyBoost` moveSpeed-multiplier design, which silently did nothing while the player wasn't already holding a move direction (common right after an updraft launch). `ApplyDash` is a genuine additive world-space velocity (along the gate's own forward, already oriented along the course) that decays linearly to zero over 0.5s, summed into `motion` independent of input state.
- **Checkpoint course second follow-up ("1.金色光環太淺太薄不顯眼不夠大 2.距離和數量分配不適當 3.達成目標後 顯示 完成任務!")**: ring texture thickened/brightened and `RingWorldSize` doubled to 8x8; `CheckpointGate.upcomingGateColor` alpha raised 0.25→0.6 so gates ahead on the path (not just the current "Next" one) stay visible; `GatePositions` switched from 6 hand-picked points to 9 evenly-sampled points (t=i/8, height eased 1-(1-t)^2) for consistent ~8-unit spacing; `TimeTrialController` now holds a temporary "完成任務！" HUD message for 3s on finish instead of silently resetting.
- **Checkpoint course third follow-up ("金色光環的光暈太嚴重了")**: the ring-thickening pass above overcorrected into a washed-out bloom - glow sigma pulled back in (0.30→0.14) and its contribution reattenuated (1.0x→0.5x), core sigma back to 0.05, keeping the doubled world size (that wasn't the complaint).
- **Checkpoint course fourth follow-up ("光圈首顆起點不對 看上升氣流的頂端 從頂端前方往天空島方向開始配置光環")**: gate 0 had been eyeballed at (-8,7,-8) - neither at `Updraft_MainArea`'s actual top nor even inside its column (its live `CapsuleCollider`: center (-2.5,5,-6), height 10, radius 2 → world top is (-2.5,10,-6); the old gate was 5.85 units off-axis and 3 units short). Recomputed from the real collider geometry: gate 0 now sits 3 units ahead of the measured top along the top→SkyIsland_Ground direction, with the remaining 8 gates re-derived from this corrected start.
- **Sky island unreachable via the new flight route, explicit user request ("空島周圍解除邊界阻擋，否則無法從外圍進入空島內部")**: `SkyIsland_Boundary` (24 invisible `BoxCollider`-only wall segments, no `MeshRenderer`, each 6.3 units tall) formed a completely closed ring around the island's entire perimeter with no gap - never a problem before since the only prior access route was `Portal_SkyToGround`, which teleports position directly and ignores colliders entirely. The new Updraft->flight route requires physically flying through this space for the first time, so the wall (originally presumably meant to stop wandering/falling off the edge) became a hard blocker with no visible cause. Deleted the whole `SkyIsland_Boundary` object - nothing else in the scene depends on it (no characters are stationed on the sky island).
- **Sky island visual clipping, explicit user request ("空島上的視覺穿透問題請解決")**: root cause was `DistantMountains` - 16 background mountain props scaled ~800-1250x (to read as tiny silhouettes from the main ground map, tens of units away) but several were positioned close enough to the sky island's own coordinates (as near as 6.4 units flat-distance) that their actual multi-hundred-unit real geometry visually overlapped/clipped through the island once approached at flight-course range - never an issue before since nothing ever got this close to them. Found by direct measurement (`Renderer.bounds` distance from island center) and confirmed via ray-vs-bounds intersection tests against real camera viewpoints, not guessing - one, `Mountain_Rock3_15`, was confirmed as the literal first thing a establishing-shot ray hit before the sky island itself. Disabled the 6 props within ~50 units of the island (`Mountain_Rock2_4/6/12`, `Mountain_Rock3_0/3/15`); the other 10 stay active for the main map's intended horizon backdrop. A speculative global fog-tuning attempt (`fogStartDistance` 35->15) was tried first, produced no measurable change once ray-tested, and was reverted rather than left in as an unjustified global side effect.
- **Player flight-energy increase re-applied and this time explicitly saved**: the same live-scene edit (Player's dedicated flight `UltimateEnergy` instance, separate from the Ultimate skill's own, maxEnergy 100->200) had been made once already in an earlier turn but was silently lost - a subsequent `SkyIslandTimeTrialSetup.Apply()` run calls `EditorSceneManager.OpenScene(...)`, which reloads the scene from the last-saved disk state and discards any in-memory edit that was never explicitly saved. Re-applied and immediately followed with `manage_scene(action:"save")` this time. General lesson: any live-scene property edit made via MCP must be saved (or made AFTER the last scene-reloading operation in a session) or a later Editor-script scene reload can wipe it without any error.

- **Sky island still un-walkable / camera clips outside the world, explicit user request ("空島上無法自由遊走 且視角移動時偶爾會穿透到場景之外")**: two distinct bugs, one pre-existing and one a direct regression from this session's own earlier fix.
  - **Can't roam freely**: `SkyIsland_Ground`, a leftover flat `BoxCollider` placeholder from before the real `Torii_FloatingIsland` terrain asset was ever placed, was still enabled and physically overlapping/fighting that asset's own proper non-convex `MeshCollider`s (`Terrain_GrassTerrain_Material_0` etc., which already match the real undulating grass/rock surface exactly, confirmed by direct bounds inspection). Two mismatched solid colliders occupying the same space read as getting stuck/blocked. Fixed by disabling `SkyIsland_Ground`'s `BoxCollider` - the real mesh colliders are sufficient alone.
  - **Camera clips past the world edge**: a direct regression from deleting `SkyIsland_Boundary` entirely in an earlier turn (to fix "無法從外圍進入空島內部") - that also removed the only geometry `ThirdPersonCameraController`'s own obstruction-avoidance `SphereCastAll` had to catch against near the island's edges (null obstruction just means "use the full desired distance, unclamped" - see that class's `FindObstructionDistance`/`ClampDistanceForObstruction`). A solid wall can't simply come back without reproducing the original flight-entry block. Fixed with `SkyIslandCameraBoundarySetup` - a 16-segment ring on a new dedicated `SkyIslandCameraBlocker` layer, with `Physics.IgnoreLayerCollision(blockerLayer, defaultLayer, true)` so the player's own `CharacterController` (walking OR flying) passes straight through, while `Physics.SphereCastAll` - a spatial query, unaffected by the layer collision matrix - still detects it and pulls the camera in. Verified directly: a radial `SphereCastAll` from near the island center hits the ring at the expected distance; `Physics.GetIgnoreLayerCollision` confirms the player layer is excluded.
  - General lesson reconfirmed: `SkyIslandCameraBoundarySetup.Apply()`'s own `EditorSceneManager.OpenScene()` reload wiped the `SkyIsland_Ground` disable edit that had been made just before running it (same pattern as the flight-energy loss earlier) - re-applied AFTER the reload and saved immediately.

- **Sky island pond pushes the player away / looking at feet shows brown ground instead of grass, explicit user request ("空島上的池塘靠近會被推開" / "視角往腳底看過去會穿磨成咖啡色大地 而非空島的綠草")**: both traced back to real, verifiable collision bugs, not guessed.
  - The island's `Terrain` and `Terrain.001` objects turned out to be an EXACT duplicate pair (identical position/rotation/scale, same parent `RootNode`) - two full copies of the whole terrain's grass/rock MeshColliders stacked on top of each other. Walking near complex geometry (the pond's edge) meant the CharacterController resolved against two slightly-non-identical overlapping non-convex mesh surfaces at once, reading as an unexpected shove.
  - Directly raycast-tested both copies individually (`Physics.Raycast` straight down at several spread-out points on the island) before touching anything: `Terrain` alone left every single test point missing the grass surface entirely and landing on `SkyIsland_UndersideBlocker` instead (the earlier turn's height-extended backstop box, now revealed as taller than intended); `Terrain.001` alone correctly hit real grass at every point (~y=21.7-21.9, consistent). So `Terrain.001` has the working, complete collision and `Terrain` does not - kept `Terrain.001` active, disabled `Terrain`.
  - This also explained the "brown ground at feet" report directly: with the (working) grass terrain's real collision effectively masked out by the previous turn's over-tall underside blocker in most places, the player was actually STANDING ON the blocker's brown-tinted box (from the earlier "visual clipping" fix) almost everywhere, not the green grass mesh at all. Once `Terrain.001` alone provides correct, complete collision, `SkyIsland_UndersideBlocker` no longer needs to reach anywhere near the walkable surface - pulled its height back down (y=8-14, well clear of the ~21.7-22 grass level) to its proper role as a deep fall-through safety net only, re-verified via the same raycast sweep (all points hit the grass terrain, none hit the blocker).
  - One small, pre-existing cosmetic gap in the terrain mesh (visible only from a narrow near-vertical overhead angle at one specific spot, first found in an earlier turn) persisted through this whole investigation regardless of which Terrain copy was active - not the cause of either reported bug, left as a known minor seam rather than chasing it further.

- **Correction to the entry above, real playtested bug caught live in Play Mode ("剛剛我在PLAY MODE模式 是否修正失效了" / player fell all the way through to underneath the island)**: the "exact duplicate pair" diagnosis above was wrong. `Terrain` and `Terrain.001` share an identical transform but are NOT duplicate meshes - they're complementary pieces, each covering a DIFFERENT part of the island's actual collision footprint (confirmed by testing each in isolation live in Play Mode: with only `Terrain.001` active, a full raycast sweep across the island found a large contiguous hole - zero floor - across the whole pond/finish-gate side; re-enabling `Terrain` alongside it brought the same sweep to 0 holes out of 110 sample points). Disabling `Terrain` (the previous turn's "fix") had left that entire region with no collision at all, which is what the player fell through mid-session. Re-enabled `Terrain` permanently (both objects active together) - since they're genuinely non-overlapping pieces, this doesn't reintroduce the original overlapping-collision "push" symptom the earlier entry was chasing; that symptom most likely came from the CharacterController hitting the hole's own edge (solid ground ending abruptly into empty space), not from double collision, and should be re-tested now that the hole is gone. General lesson: Play Mode testing (when actually available, unlike the usual Time.time-freeze limitation) is strictly more trustworthy than Edit-Mode-only raycast spot-checks - a handful of sample points that happen to land on the correct piece's coverage can look like confirmation of the wrong theory entirely.

- **Pond push, real root cause finally found ("沒修好 還是被推開")**: not collision overlap or a hole edge as earlier entries guessed - directly sampled `hit.normal` angles (`Vector3.Angle(normal, Vector3.up)`) across the pond area via raycast instead of guessing again. `RockExtra1_Toro_Material_0`/`RockExtra2_Toro_Material_0` - small decorative accent rocks bordering the pond - have locally steep sides (measured up to ~78°, well past `CharacterController.slopeLimit`'s 65°). `CharacterMovement`'s existing (correct, intentional) `GroundSlopeUtility.IsTooSteepToStandOn` anti-standing-on-steep-terrain system was firing exactly as designed - it just happened that the pond's only decoration was modeled steep enough to constantly trigger it right where a player would naturally walk along the shore. Fixed by disabling just these two rocks' `MeshCollider`s (visual meshes untouched) - same "small decoration doesn't need to be solid" precedent bamboo already has elsewhere on the island. Re-verified: both rock footprints now correctly fall through to the main terrain layer underneath (~1.1° - flat, walkable, no new hole), and a full re-sweep of the pond area found zero >65° trigger points remaining nearby (the only 3 that still exist are on the island's outer boundary edges, unrelated to the pond).

- **Two more push-related reports, both verified with the same normal-angle raycast method ("直接飛上去不會被推開 使用傳送門則會 池塘上仍然有一些部分區域會推開")**:
  - **Portal arrival push**: `Portal_GroundToSky_Root`'s `arrivalOffset` landed the player only ~1.5-2 units from a genuine steep cliff face in the main terrain (`Terrain_RockTerrain_Material_0`, ~84°) - not decoration this time, part of the bulk terrain, so disabling collision wasn't an option. Mapped the flat/steep region around the portal with a raycast grid, found a clean pocket (~1.2 unit safety radius, no steep points or holes) at `arrivalOffset` (-2.5, 0.1, -1) instead of the original (-1.5, 0.1, -0.12), and moved the landing point there. Flying in via the checkpoint course never had this problem since the course's own gates were already placed on verified-clean ground.
  - **Remaining pond spots**: a wider raycast sweep found `Rocks.001_Toro_Material_0` (a third small "Toro_Material" decorative rock, same family as `RockExtra1/2`, ~10 units from the pond itself but within the island's broader rocky area) also had a locally steep face (72°) - disabled its `MeshCollider` too, same reasoning as the first two. Re-swept a 3-unit radius directly around the pond afterward: 3 remaining steep sample points out of 625 (0.5%), all on the main terrain's own bulk rock mesh (not a small prop) - a single small, likely unavoidable-without-remodeling terrain corner, down from a much larger problem before; left as a known minor edge case rather than touching bulk terrain collision again.

- **Full-island walkability audit, explicit user request ("嚴格檢視整個空到目前得可行走區域")**: swept the whole island footprint (X -88..-52, Z -45..-5) at 0.75-unit resolution checking both holes and >65° normal angles, then drilled into specific reports at finer resolution.
  - **"神社底下那一面走過來就會被推開"**: found and confirmed via fine sweep - a narrow (~1.2 unit), shallow (~0.5 unit deep) V-shaped decorative crack/groove in the main terrain (`Terrain_RockTerrain_Material_0`) runs diagonally right through the direct walking corridor between the shrine/torii area and the pond (roughly X[-70.8,-64] Z[-27,-18.2]) - its two slopes are steep (~74-75°), so crossing it head-on (the natural shrine-to-pond route) triggered the push; approaching the pond from other directions never crosses it, matching "從正面...不會" exactly. Since it's bulk terrain (not disposable decoration), fixed with a new invisible flat `BoxCollider` bridge (`SkyIsland_ShrinePondCrackBridge`, top surface at the crossing zone's highest measured point) spanning the whole crack's bounds rather than trying to trace its winding shape - re-swept afterward, 0/744 steep-or-hole points remain in that whole region.
  - **Portal push, re-verified but not yet reproduced**: re-derived the ACTUAL landing position using Portal.cs's real formula (not just `destination.position + arrivalOffset` - it also adds `controller.height * 0.5 * lossyScale.y` for the capsule's world center, a detail missed in the earlier fix's own verification), replicated `CharacterMovement.TryGetGroundNormal`'s exact `SphereCastAll` at the settled landing spot, and confirmed 0° - perfectly flat, no overlap either via `Physics.OverlapCapsule` matching the player's real capsule shape at a range of heights. Could not reproduce a landing-site cause this time; flagged for the user to retest and report whether the push happens the instant they land or only after moving in a specific direction (which would point to a different, not-yet-found spot rather than the arrival point itself).
  - Full-island re-sweep after the crack bridge: 34 remaining steep sample points, all either the island's own natural outer cliff edges (32, `Terrain_RockTerrain_Material_0`, scattered thinly around the whole perimeter - expected, not a real path) or solid structural elements you shouldn't be able to walk through anyway (1 torii pillar `Hashira_Red_Material_0`, 1 `Rocks_Toro_Material_0` sample). None of the "holes" in the coarse sweep are real gaps - confirmed separately via three independent finer sweeps (110-point spread, 3-unit pond radius, shrine-pond corridor) that the actual walkable footprint has zero holes; the coarse sweep's holes are just sample points landing off the island's own irregular edge into open air.

- **Sky island pond camera clipping when looking down, explicit user request ("空島地區的池塘視角穿模問題，先把角色放在池塘上，然後實際走動且視角往地下看排除問題")**: reproduced and root-caused live in Play Mode rather than guessed - teleported the player onto the pond, then forced `ThirdPersonCameraController`'s private `_pitch` to 68° (near `maxPitch=70`, steep look-down) via reflection and raycast along the camera's own actual forward direction to see exactly what it was rendering. That raycast hit `SkyIsland_ShrinePondCrackBridge`'s `BoxCollider` first (the crack-bridge from the audit entry above - a 9.1×9.0 unit flat platform at y=21.9) - but that object has no Renderer at all, and the real ground beneath it (`Terrain_RockTerrain_Material_0`) sits ~0.9-1.0 unit lower (confirmed at 5+ sample points spread across the bridge's whole footprint, including the pond center). The player was physically standing on this invisible platform floating above the visible terrain the whole time; looking steeply down let the camera's view pass straight through the (collision-only, unrendered) platform to the real ground visibly below the character's feet - exactly the reported "穿模". Disabled the bridge collider and re-swept its entire footprint at 0.15-unit resolution (2714 points) with it off: zero holes, zero interior steep(>65°) points - only the island's own already-accepted outer cliff edges - so whatever crack this bridge was built for no longer reproduces at this resolution, and the pond area is fully covered by its own real mesh colliders without it. Fixed by disabling the bridge's `BoxCollider` (kept the GameObject - same "disable rather than delete" precedent as `SkyIsland_Ground` above), then re-verified with real `CharacterController.Move` walks (not teleports) looping around the pond shore and through its center, camera pitched steeply down throughout: `grounded=true` the entire way, a steady ~0.08 feet-to-ground gap (the controller's own `skinWidth`, expected), and the camera's forward ray now hits the real rendered terrain directly with nothing invisible in between. Saved the scene, then re-entered Play Mode fresh and re-ran the same walk+look-down check to confirm the fix actually persisted rather than being a Play-Mode-only edit discarded on Stop.

- **Follow-up, explicit user request ("在池塘邊時依舊穿模 理論上看見的應該要是綠色草地")**: the floating-collider fix above was correct but incomplete - a genuinely different bug at the pond's actual shore (not the crack-bridge area). Reproduced with an actual rendered screenshot this time (RenderTexture capture read back into a PNG), not just physics raycasts, since this turned out to be a pure rendering bug raycasts can't see: standing right at the grass/pond boundary and looking down showed a solid brown, cracked surface instead of grass. Traced triangle-by-triangle rather than guessed: the grass mesh's own geometry AND collider both genuinely cover that exact spot (verified same shared `Mesh` asset for `MeshFilter`/`MeshCollider`, an upward-facing normal, and the material's real `baseColorFactor` = a saturated green) - it just never got drawn from that camera angle. Root cause: `Terrain.001_GrassTerrain_Material_0` (and several sibling greybox meshes - the rock terrain, the water disc) come back with a completely degenerate `Mesh.bounds` (zero size, sitting at the mesh's own local origin far from its real ~25-unit footprint) - these are glTF-imported meshes and whatever produced this project's FBX apparently never ran `RecalculateBounds()`. Unity's frustum culling trusts that broken bounds over the real vertex data, so any camera angle that doesn't happen to contain that arbitrary origin point gets the WHOLE renderer silently culled - which is exactly what "looking down at the pond shore" does. With the grass renderer culled away, the camera saw straight through to the rock terrain mesh's own backface (a downward-facing, `Cull Off` underside triangle sitting near `SkyIsland_UndersideBlocker`'s depth) instead - the "brown, should be green" report. Fixed with a new runtime component (`MeshBoundsFixer`, `Live2DAction.World`) rather than a one-off Editor tool, since a Mesh bounds fix applied only in the Editor doesn't survive - it's FBX/glTF sub-asset data, regenerated with the same broken bounds on every reimport: `[ExecuteAlways]`, walks every `MeshFilter` under `Torii_FloatingIsland` on `Awake`/`OnEnable` and calls `RecalculateBounds()` on any mesh whose bounds come back exactly zero despite having real vertex data, so it self-heals every time the scene actually loads (Editor preview or Play). Re-verified with the same screenshot method at the same spot after adding it: the pond shore now renders as green grass.

- **Both pond fixes above written up as a standalone reference, explicit user request ("紀錄前後改動差異 核心原因 和核心解決方法以及此空島當前設定,以利於後續島嶼新建開發")**: `Docs/FLOATING_ISLAND_GUIDE.md` - before/after diffs for both fixes, the two root causes (collider/renderer height mismatch; degenerate glTF-imported `Mesh.bounds` breaking frustum culling) explained generally (not just this one incident), and a live-read snapshot of this island's actual current setup (`Terrain`/`Terrain.001` complementary collision, `SkyIsland_UndersideBlocker`'s real bounds, `SkyIslandCameraBoundary`'s ring parameters, disabled decorative-rock colliders, the pond's real mesh AABB) plus a checklist for building the next floating island without re-discovering the same pitfalls.

- **Checkpoint course cleanup, three separate explicit user requests in one turn ("空島上兩個光還請移除" / "第一個光環比上升氣流的頂端還要低，沒有辦法穿過去，後面的光環也要對應調整" / "玩家飛行時的耐力條很快就沒了")**:
  - **Two gates removed from the island itself**: confirmed via an orthographic top-down screenshot (an earlier oblique-angle shot was misleadingly ambiguous about which rings were "on" the island due to perspective foreshortening - don't trust a skewed camera angle for this kind of position judgment again, use orthographic top-down or check world-space X/Z against the island's known footprint directly) that the old gates 7/8 (X=-60.17/-68, both inside the island's established X∈[-88,-52] footprint) were the only two actually sitting on/at the island, while 0-6 are genuinely in open air. Removed them from `SkyIslandTimeTrialSetup.GatePositions` entirely (now 7 points, 0-6) - the timed course now ends just short of the island's edge (what was gate 6) rather than including the actual landing.
  - **Gate 0 raised clear of the updraft**: `Updraft_MainArea`'s real `CapsuleCollider` top is world Y=10 (center local (0,5,0) + height 10, read live off the scene) - the old gate 0 (Y=10.56) was only 0.56 above that, and the user's actual playtest confirmed it couldn't reliably be flown through despite the geometry suggesting it should just barely work. Raised to Y=12 (2 units of real clearance) and re-eased every remaining gate's height off that new baseline with the exact same `1-(1-t)^2` curve as before (t=i/8, now i=0..6) so the whole climb re-bases higher as one continuous shape rather than kinking at gate 0 alone.
  - **Player flight energy drain halved**: `CharacterMovement.flightEnergyDrainPerSecond` 20→15. Drain fires the whole time `_isFlying` is true (ascend/descend/hover all included, not just climbing) and nets directly against `UltimateEnergy`'s own passive regen (10/sec) running independently - net cost was -10/sec against a 200 max, ~20 seconds of continuous flight, short next to how long the sky-island route actually takes. Now nets to -5/sec (~40 seconds) - still a real, felt resource, not reduced to zero net drain.

- **Full RPG-style free-flight system, `/grilling`-designed then implemented from `Docs/FLIGHT_SYSTEM_DESIGN.md`** (that doc carries the full design-tree rationale; only outcomes summarized here): flight horizontal speed decoupled from ground `moveSpeed` (own `flightMoveSpeed`), vertical ascend/descend eased via `SmoothDamp` instead of snapping instantly, a dedicated Q-held boost (`boostSpeedMultiplier`, extra energy drain, Flying-only), a dive-speed bonus requiring BOTH camera pitched down past a threshold AND actively holding descend, and banking (`CurrentBankRollDegrees`) driven by strafe-input strength, applied to both the character's Visual child and (synced, single source of truth) the camera's own roll via a new `ICameraYawSource.PitchDegrees`.
  - **Real playtested bugs found and fixed the same day**: (1) holding Ctrl from the ground vibrated in place instead of taking off - the new eased ascend took several frames to build real velocity, and the flight-exit check's `_controller.isGrounded` alone kept reading true during that ramp and cancelling flight every single one of those frames; fixed by requiring `!flyHeld` too (`landed = isGrounded && !flyHeld`), so holding the ascend key rides straight through the transient reading. (2) Glide (a soft fixed-rate fallback when energy ran dry) removed entirely on explicit feedback ("體力條歸0時要快速掉落到地面 停止飛行") - running out of energy now ends `_isFlying` immediately and falls under real accelerating gravity, same as any other fall; `IsGliding`/`glideDescendSpeed` and every reader of them (`ThirdPersonCameraController`, `CharacterAnimatorLink`) removed to match. (3) `maxBankRollDegrees` halved 20→10 (real feedback: too much sway). All three needed the live scene's already-serialized `Player` component values patched directly via `SerializedObject` too, not just the C# defaults - the by-now-familiar "a code default change alone doesn't reach an already-saved GameObject" lesson, again.
  - **Flight energy economy redesigned again, separately, explicit user request ("設計為飛行體力500 只有在閒置3秒沒有消耗體力後才會逐漸恢復體力 恢復速度提高")**: `UltimateEnergy` (shared generic class, also used by the ultimate skill - see that class's own header comment) gained an opt-in `regenIdleDelaySeconds` (default 0, so the ultimate-skill instance is completely unaffected), reset on every `Drain()` call - passive regen only resumes once genuinely idle for that many seconds, replacing the old "drain and regen just net against each other continuously" model. Flight instance: `maxEnergy` 200→500, `regenAmount` 10→30/sec, `regenIdleDelaySeconds`=3. Verified both the gating (no regen mid-drain, regen resumes past the 3s mark) and that the ultimate-skill instance's own always-on regen was untouched, via direct reflection-driven calls to the real `Drain()`/`Update()` methods rather than guessing at the timing logic.
  - **Enemy locomotion animation, explicit user request ("敵人的移動動作採用跟玩家一樣地踏步")**: Enemy never had its Speed parameter driven at all - `CharacterAnimatorLink` used to hard-`RequireComponent(CharacterMovement)`, which only Player has (`EnemyAI` is a deliberately separate movement implementation - see that class's own header comment). Generalized via a new `ICharacterSpeedSource` interface (same "small interface, implemented by whichever concrete class this instance has" idiom as `IInputCommand`/`ICameraYawSource`) that both `CharacterMovement` and `EnemyAI` now implement; `CharacterAnimatorLink` resolves it via `GetComponent<ICharacterSpeedSource>()` instead of the concrete type. New `Tools/Live2DAction/Wire Character Animator Link On Enemy` menu item (mirrors the existing Player one) applied and verified - Enemy's Animator "Speed" parameter now genuinely tracks its own `_horizontalVelocity` magnitude.
  - **Camera auto-pitch while descending, explicit user request ("玩家飛行下淺的視角也要跟隨壓低")**: new `enableDescendAutoPitch` on `ThirdPersonCameraController`, same gated-easing idiom as the existing auto-center yaw (cedes control the instant real mouse input arrives) but keyed off a new `CharacterMovement.IsDescending` (Flying + holding descend) instead of "walking forward/back", and with no onset delay (holding descend is already a deliberate action, unlike incidental walking). Deliberately complements the dive-speed bonus above rather than duplicating it - that one reacts to the camera already being pitched down while descending; this one is what actually gets the camera pointed down in the first place, so the two now reinforce each other instead of requiring the player to manually aim down first.

- **"本地" interior partially blocked, explicit user request ("我發現本地內部一部份區域被阻擋 我要的是本地的邊界才要阻擋")**: swept the whole Ground footprint (X/Z ∈ [-15,15], 0.75-unit resolution, triggers ignored) rather than guessing which object - found exactly one anomaly: `Pagoda_ClimbRamp` (a `JapaneseShrineVista` child, collider-only with no renderer at all, heavily rotated) had a world-space bounding box reaching from X=-25.97 to X=-10.57 - most of it sits outside 本地 entirely, but roughly 4.4 units of it (up to X=-10.57) intruded past the West boundary wall into open interior ground, blocking a chunk of it with nothing visible there to explain why. No record of this object in any past session's notes - not a regression from anything done here, just a pre-existing leftover. Fixed the same way this project always handles a stray collider that turns out to be in the way: disabled its `BoxCollider` (kept the GameObject, no renderer to lose anyway) rather than deleting it. Re-swept afterward (1681 points): only the four `BoundaryWall_*` and each character/prop's own small footprint remain - no more unexplained interior blocking.

- **Enemy stuck directly underneath the player during Aerial Combat, explicit user request ("我如果在敵人正上方直線往天空飛行 敵人大概率會卡在我腳底下...讓他繞開腳下直接到我正面進行攻擊")**: root cause traced (not guessed) via a reflection-driven repro - `EnemyAI`'s horizontal Aerial Combat chase aims straight at the player's own flattened (Y=0) XZ position; the instant the player is close to directly overhead, that horizontal offset shrinks toward zero, `direction` degenerates to `Vector3.zero`, and `_horizontalVelocity` goes to zero right along with it - Enemy just climbs straight up with no horizontal correction at all, arriving directly beneath the player instead of approaching. Confirmed directly: forcing this exact scenario (player teleported straight above an airborne Enemy) reproduced `_horizontalVelocity=(0,0,0)` before the fix. Fixed by giving Aerial Combat's horizontal chase a different target than ground combat's: a point offset a new `aerialApproachDistance` (2f) in FRONT of the player (`target.position + target.forward * aerialApproachDistance` - the player's root only ever yaws, per `CharacterMovement`'s own facing block, so this is already purely horizontal) instead of the player's own position - never coincident with "directly below the player" the way the raw position is, so it can't degenerate the same way, and it's not just a degenerate-case patch: it makes Enemy approach from the front generally, matching what was actually asked for beyond fixing the one reported angle. Facing/`MoveInput` still track the player's real position unchanged - only the movement target changes, Enemy still visually looks at what it's attacking rather than where it's walking. Re-verified after the fix: same exact repro now gives `_horizontalVelocity=(0,0,2.4)` (full `aerialHorizontalSpeed`, pointed at the player's forward direction) instead of zero.

- **HealingSpring extended to also refill flight energy, explicit user request ("請讓泉水點也支援回復體力條")**: `HealingSpring` already refilled Health and the ultimate skill's `UltimateEnergy`, but Player carries a SECOND, separate `UltimateEnergy` instance for flight (wired independently, see `CharacterMovement.flightEnergy`'s own comment) - a plain `GetComponentInParent<UltimateEnergy>()` can't tell the two apart, so flight energy was never touched. Added `CharacterMovement.FlightEnergy` (a public accessor for the already-existing private reference - same "expose what an external system needs" idiom as `IsFlying`/`CurrentBankRollDegrees`) so `HealingSpring` can resolve the flight instance unambiguously via `GetComponentInParent<CharacterMovement>()?.FlightEnergy`. New `flightEnergyPerSecond` (100/sec, its own rate rather than reusing the ultimate skill's 40/sec - flight energy's 500 max is 5x bigger, so the same flat rate would fill 5x slower and stop reading as "快速"; 100/sec matches this same class's already-established "5 seconds from empty" pace for Health). `AddEnergy` doesn't touch the flight energy's newer idle-gated passive regen (`regenIdleDelaySeconds`/`_timeSinceLastDrain`) at all - confirmed by design (that field's own comment already says direct top-ups are independent of the passive tick) - so the spring's fast refill and the flight system's "must be idle 3s" passive regen don't fight each other. Verified via a direct reflection-driven `OnTriggerEnter`+`Update()` call against the real Player/HealingSpring instances: flight energy rose from a forced 50 to 70 over 10 ticks, and confirmed the resolved instance's own `maxEnergy` reads 500 (the flight one), not 100 (the ultimate skill's).

- **Flight descend tuned faster, real playtested feedback ("俯視飛行似乎沒辦法做到真的低或直直落下")**: measured the actual mechanic directly rather than assuming a logic bug - a plain straight-down descend (no WASD, level camera) reaches ground level and lands cleanly with no stall/cutoff, and even a FULLY maxed dive (camera pitched to `maxPitch`, holding descend) only reached the OLD `flightDescendSpeed(4) * diveMaxSpeedMultiplier(1.4)` = -5.6/sec - slower than plain ascend (6) and far behind horizontal cruise (9, let alone boosted 16.2). Nothing was actually broken; diving just never felt fast or committed next to the rest of the kit. `flightDescendSpeed` 4→6 (now matches ascend as the plain-descend baseline, not weirdly slower), `diveMaxSpeedMultiplier` 1.4→2.5 (full dive now reaches 15/sec, close to boosted horizontal - committing to a real look-down dive now has a dramatic payoff instead of a marginal one; the existing auto-pitch, which only eases to 45° not the full 70°, still gets a meaningfully fast ~11/sec partial dive without the player needing to manually aim all the way down). Both fields were already live-serialized on Player from the original flight-system pass, so needed the usual direct `SerializedObject` patch, not just the C# default change - re-verified in a fresh Play session afterward (measured steady-state -15/sec on a full dive, confirming the scene actually picked up the new values, not just the source).

- **Immediate follow-up, real playtested bug ("現在我明明直直地面對前方還會下墜 或許跟攝影機視角有關?")**: the user's own guess (camera-related) wasn't quite it, but the actual mechanism was directly measurable rather than guessed - releasing descend after a full dive left a MUCH bigger residual `_verticalVelocity` (now -15, from the tuning above, vs the old -5.6) to bleed off through the single shared `flightVerticalSmoothTime`(0.18s) - measured directly: it took ~20 simulated ticks (~0.4s) to actually settle back near 0 after letting go of everything and leveling the camera, which reads exactly like "I'm looking straight ahead, holding nothing, and still falling" even though it's genuine (if now much larger) momentum, not a new bug in the strict sense - just my own previous tuning pass making an existing coast-time far more noticeable. Fixed with a new `flightVerticalStopSmoothTime` (0.08s, much shorter) used specifically when NEITHER ascend nor descend is held (mirrors the horizontal velocity's own existing accel/decel split, `accelerationSmoothTime` vs `decelerationSmoothTime`) - diving/climbing still ease in via the original (longer) `flightVerticalSmoothTime`, only "letting go and returning to hover" got faster. Re-measured the same full-dive-then-release scenario: settles near 0 in ~10 ticks (~0.2s) now, roughly half the coast time.

- **Still "falling while facing forward" after the momentum fix above, explicit user request ("還是一樣 請設計程式視角往下shift時才會往下飛行")**: the momentum-decay fix addressed the wrong scenario - the user was holding Shift the ENTIRE time with a level camera, and the original design's own rules correctly (by ITS rules) kept descending regardless of camera angle (camera pitch was only ever a speed BONUS on top of an already-happening descend, never a precondition for it) - not a bug under the old design, just not the behavior wanted. Explicit new instruction implemented literally: `diving` (already existed as the dive-bonus condition - camera pitched past `divePitchThresholdDegrees` AND holding descend) now GATES whether descend happens AT ALL, not just how much bonus it gets - holding Shift with a level or upward-looking camera does nothing now (falls through to hover, target vertical = 0), only holding it while the camera is genuinely pitched down past the threshold actually moves the character downward. `IsDescending` (drives the existing descend-auto-pitch) deliberately stays keyed to the raw `flyDescendHeld` signal, not `diving` - gating it on `diving` would be a deadlock (the auto-pitch's whole job is easing the camera down PAST that same threshold, so it has to be able to start before the threshold is crossed). Verified directly: holding Shift for 30 ticks with the camera held level gives `_verticalVelocity=0` exactly (no descend at all); the same hold with the camera pitched to 30° (past the 15° threshold) gives a clearly negative velocity within the same window.

- **Follow-up, terse feedback ("不夠") clarified via a direct multiple-choice question rather than guessed** - the user picked neither of the two obvious readings (threshold too strict / descend too slow); their actual answer was the OPPOSITE of what seemed most likely from the previous entry's framing - "門檻太低 太容易觸發下淺" (the gate itself was working, but 15° is barely a tilt, so it kept firing sooner than felt deliberate). `divePitchThresholdDegrees` doubled 15°→30° - still comfortably under the descend-auto-pitch's own 45° target (still crosses the new threshold on its own) and leaves real headroom under `maxPitch`(70°) for `diveMaxSpeedMultiplier` to keep scaling. Already live-serialized on Player from the original pass, so needed the usual direct `SerializedObject` patch too. Re-verified: pitch=20° (used to trigger descend under the old 15° threshold) now correctly gives `_verticalVelocity=0`; pitch=40° (past the new 30°) still descends normally.

- **Real playtested bug, explicit user report ("我打鏡頭抬到最上方 為何SHIFT還會往下")**: the descend-auto-pitch feature (added earlier the same day) used to fire purely off "is Shift held" (`IsDescending`), with zero regard for which way the player had just manually pitched the camera. The instant the mouse stopped moving - even the very next frame after deliberately pitching all the way up to `minPitch`(-40) - while still holding Shift, this yanked the camera straight back down toward `descendAutoPitchTargetDegrees`(45°), silently overriding the player's own explicit look-up, and once pitch crossed the (now load-bearing, gating) dive threshold, real descend followed right along with it - exactly the reported symptom. Since camera angle now GATES whether Shift descends at all (not just a speed bonus, per the two entries above), an auto-assist that silently defeats the player's own opposite camera choice defeats the entire point of that gate. Added `_pitch >= 0f` to the auto-pitch condition - it now only ever assists from level or already-somewhat-down, and backs off entirely the moment the player has pitched up at all, leaving the camera exactly where they put it. Re-verified both directions: holding Shift with the camera pinned at `minPitch` for 60 ticks now leaves pitch AND vertical velocity completely unchanged (0/0 the whole time); the original from-level auto-assist still works unchanged (0° → ~41° over the same window when starting level).

- **"本地" still had invisible walls after the `Pagoda_ClimbRamp` fix, explicit user request ("本地區域內仍然有一些看不見的牆 排除他們")**: the earlier fix wasn't wrong, just incomplete - this was a genuinely different culprit the previous ground-sweep's resolution/scope happened to miss. A finer 0.5-unit `Physics.OverlapBox` sweep at player-torso height (not a straight-down raycast this time - deliberately switched methods since the raycast-based sweep had just come up clean) found a real, physically-blocking, completely unnamed (`""`) `CharacterController` sitting at (8, 1.98, 12), plus a second unnamed non-colliding Cubism model 6 units away at (14, 1.93, 12). Investigated rather than deleted on sight: the first one turned out to be a FULLY wired second AI enemy - full Cubism ArtMesh rig, `Health`/`PlayerCombat`/`EnemyAI`(`target`→Player, correctly chasing)/`HealthRegeneration`, and its own `RespawnController` slot on `GameManager` - not inert junk. Traced it against the project's own naming history (`EnemyAISetup.cs`'s class comment: "the object described here is long gone" - the codebase's current, explicit position is that there is exactly ONE `Enemy` now, the simple-capsule one `GreyboxSceneBuilder.CreateEnemy` still creates) - so a second, fully-functional, unnamed duplicate directly contradicts the documented current design, confirming this is leftover clone content rather than intentional. Confirmed both objects were genuinely orphaned before touching anything: a full `SerializedObject` scan of every `MonoBehaviour` in the scene for object-reference fields pointing at either instance found exactly one real reference (the `RespawnController.target` on `GameManager`) and nothing else anywhere. Removed that one `RespawnController` component, then destroyed both GameObjects, then saved. Re-verified with a fresh Play session: the same OverlapBox sweep now returns zero unknown colliders across the whole Ground footprint; `GetComponentsInChildren<Transform>(true)` from every scene root confirms zero remaining empty-named GameObjects anywhere (not just at those two spots); console shows the same pre-existing, unrelated "missing script on Visual" warnings both before and after (unchanged count - not something this fix touched); EditMode suite still 133/134 (the one failure is the same pre-existing, unrelated `CharacterAttackAnimationLinkTests` mismatch noted in earlier entries).

- **Structural fix for the recurring "internal blocking" bug class, explicit user request ("你這個內部的阻擋設計很有問題 我要的是Ground這個物件的外圍做一道隱形的牆壁 且碰撞時有特效")**: first confirmed the thing being asked for (a boundary wall on Ground's own perimeter, with a touch effect) already exists exactly as described - `BoundaryWall_North/South/East/West` sit precisely on Ground's own ±15 footprint (`GreyboxSceneBuilder.CreateBoundaryWalls`'s own `halfExtent`), and each already carries a padded trigger + `BoundaryBlockEffect` (world-space ripple at the contact point) + `BoundaryBlockHud` (screen-edge vignette pulse) from an earlier session's identically-worded request - live-checked, all four still fully wired. The actual complaint was the pattern behind the last three fixes (`Pagoda_ClimbRamp`, then two orphaned duplicate characters): every one of them was a DIFFERENT root cause, found only by physically walking into it, because nothing in the scene's architecture prevents an arbitrary collider from blocking a character - every object defaults to the same `Default` physics layer, so a single forgotten/stray collider anywhere is a live bug by default rather than an opt-in one. A single exhaustive audit (every non-trigger `Collider` in the ENTIRE loaded scene via `FindObjectsOfType`, any height - not just ground/torso level like the earlier targeted sweeps - filtered to ones whose XZ bounds overlap Ground's interior) came back with zero unknown blockers right now, confirming this wasn't yet another live instance, and reframed the ask as "make this whole bug class structurally impossible" rather than "find the next one." Added a new Physics Layer (`Scenery`, slot 10 in `ProjectSettings/TagManager.asset`) and set `Physics.IgnoreLayerCollision(Scenery, Default, true)` (persisted into `ProjectSettings/DynamicsManager.asset`'s `m_LayerCollisionMatrix`, confirmed by diffing the file) - any collider on this layer can no longer physically block or trigger-detect against the `Default` layer every character/`CharacterController` lives on, regardless of whether someone remembers to disable it. Moved every purely-decorative hierarchy (no gameplay component anywhere in it, confirmed individually rather than by a blanket "no gameplay component = decoration" rule, since that rule alone would have wrongly caught actual walkable/functional geometry like `SkyIsland_Ground`/`SkyIsland_UndersideBlocker`/`SkyIsland_ShrinePondCrackBridge` - those were deliberately left untouched, out of scope for 本地 anyway) onto it: `JapaneseShrineVista` (133 objects, including the already-disabled `Pagoda_ClimbRamp` - now double-protected even if its collider is ever re-enabled by accident), `BackgroundTerrain`, `DistantMountains`, `MidDistanceTrees`, `BackgroundScenery`, `FemaleStandee_Placeholder`, `GenshinSwordDisplay_DoNotShip` (306 objects total). Checked for collateral damage before applying: `Main Camera.cullingMask` is `-1`/Everything (a new layer bit renders automatically, nothing goes invisible), and `ThirdPersonCameraController` has no hardcoded `LayerMask` field that could now miss these objects for camera-obstruction purposes (its own SphereCast uses the physics-default mask, which includes new layers unless explicitly excluded). Verified the fix actually works, not just that it compiles: force-re-enabled `Pagoda_ClimbRamp`'s collider mid-Play-session (normally kept disabled) and drove the Player's real `CharacterController.Move()` straight through its bounds - moved the full requested 1.0 unit with zero resistance (`Physics.GetIgnoreLayerCollision` confirmed true between the two layers), proving a decorative collider left enabled by mistake genuinely cannot block a character anymore, then restored it back to disabled. Re-ran the full any-height audit afterward (still 0 flagged) and confirmed `CoverBlock1` (an intentional gameplay obstacle) is untouched - still on `Default`, still not ignored against the player, still blocks as designed. Also found and removed two unrelated pieces of scene-file pollution while doing the full-scene audit: `FakeAerialTarget_TEMP` (an inert, collider-less leftover test object, name says it all) and five baked-in `HitEffect(Clone)` root objects (runtime combat particle clones that should never have survived past a Play session, apparently captured by a scene save while Play Mode was still active in some earlier session) - both harmless (no colliders) but exactly the kind of stray leftover this whole investigation was about, so cleaned up rather than left. Console warnings unchanged before/after (same pre-existing, unrelated "missing script on Visual" count); EditMode suite still 133/134, same single pre-existing unrelated `CharacterAttackAnimationLinkTests` failure.
